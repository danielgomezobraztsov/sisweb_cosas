openapi: 3.1.0
info:
  title: API Cine
  description: API Cine
  version: 0.1.0

paths:
  /sesiones:
    get:
      summary: GET todas las sesiones
      description: GET todas las sesiones
      parameters:
        - in: query
        - name: 
  
  /sesiones{id}:
    get:
      parameters: 